---
name: gc
description: Rewrite text when the user's message starts with "gc:".
---

Rewrite everything after `gc:`.

## Rules

- Fix grammar, punctuation, and natural flow.
- Preserve the writer's voice; if the structure is awkward, improve it without changing that voice.
- Use standard punctuation only (hyphens instead of em dashes).
- Output the revised text only, with no added content, no extra explanations.
