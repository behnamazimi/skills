Your arguments set the level twice: `level=A2` and `level=B2`. These produce different glossaries, so I don't want to guess:

- **A2**: beginner scaffolding. Short, simple definitions, core conversation gear (ser/estar, pronouns, negation, questions, querer/poder/tener que), no IPA by default, and controversy fields mostly left out.
- **B2**: no scaffolding. Traps, register, and constructions that still block fluent comprehension, IPA at the end of each definition, and a controversy scan.

Which level should I use? If you meant a range instead, say `level=A2–B1` or `level=B1–B2`. For example:

`Spanish | 10 | level=B2`
